# 本地开发时留空，Vite 会把 /api 代理到 http://localhost:8000。
# 如果前后端不在同一台机器，再填写后端 origin，例如：
# VITE_API_BASE_URL=http://127.0.0.1:8000
VITE_API_BASE_URL=

# 只有启用真实 Turnstile 时才需要填写；本地开发可留空。
VITE_TURNSTILE_SITE_KEY=

# 本地演示登录可设为 true，不需要验证码或后端登录接口。
# 正常联调/比赛演示建议设为 false，并使用可用的后端数据库账号。
VITE_DEMO_MODE=true
